<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Dashboard – NamasteFit</title>
    <link rel="stylesheet" href="/style.css" />
    <script
      src="https://kit.fontawesome.com/bb7a749298.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <%- include('partials/nav') %>

    <main class="admin-section">
      <h1>Welcome, <%= name %></h1>

      <!-- Available Programs -->
      <section class="admin-panel">
        <h2>Available Programs</h2>
        <div class="program-list">
          <% programs.forEach(program => { %>
          <div class="program-card">
            <h3><%= program.title %></h3>
            <p><%= program.description %></p>
            <p><strong>Duration:</strong> <%= program.duration %> mins</p>
            <p><strong>Price:</strong> $<%= program.price %></p>
            <a href="<%= program.videoUrl %>" class="btn small" target="_blank"
              >Watch</a
            >
          </div>
          <% }) %>
        </div>
      </section>

      <!-- Subscription History -->
      <section class="admin-panel">
        <h2>Subscription History</h2>
        <div class="tbl">
          <table>
            <thead>
              <tr>
                <th>SN</th>
                <th>Plan</th>
                <th>Amount</th>
                <th>Date</th>
                <th>Status</th>
                <th>Remaning Days</th>
                <th>Entry Card</th>
              </tr>
            </thead>
            <tbody>
              <% let count = 1; subscriptions.forEach(sub => { %>
              <tr>
                <td><%= count %></td>
                <td style="width: 40%; text-align: left"><%= sub.plan %></td>
                <td>$<%= sub.amount %></td>
                <td
                  class="local-date"
                  data-date="<%= sub.createdAt.toISOString() %>"
                >
                  <%= sub.createdAt.toISOString().split('T')[0] %>
                </td>
                <td
                  class="status"
                  data-status="<%= sub.createdAt.toISOString() %>"
                >
                  <%= sub.status %>
                </td>
                <td
                  class="expiry"
                  data-expiry="<%= sub.createdAt.toISOString() %>"
                >
                  <%= sub.createdAt.toISOString().split('T')[0] %>
                </td>
                <td>
                  <a href="/card"><i class="fa-regular fa-address-card"></i></a>
                </td>
              </tr>
              <% count++ }) %>
            </tbody>
          </table>
        </div>
      </section>
    </main>

    <%- include('partials/footer') %>
    <!-- <script>
      // This function converts UTC to Nepal time and updates the DOM
      function convertDatesToLocal() {
        const dateElements = document.querySelectorAll(".local-date");

        dateElements.forEach((el) => {
          const utcDateStr = el.getAttribute("data-date");
          if (utcDateStr) {
            el.textContent = new Date(utcDateStr).toISOString().split("T")[0];
          }
        });

        const expiryElements = document.querySelectorAll(".expiry");
        const status = document.querySelectorAll(".status");

        expiryElements.forEach((el) => {
          const utcDateStr = el.getAttribute("data-expiry");
          if (utcDateStr) {
            const originalDate = new Date();
            const oneMonthLater = new Date(utcDateStr);
            oneMonthLater.setMonth(oneMonthLater.getMonth() + 1);

            const diffTime = oneMonthLater - originalDate; // difference in milliseconds
            const diffDays = diffTime / (1000 * 60 * 60 * 24); // convert to days

            el.textContent = `${Math.round(diffDays)} days`;

            status.forEach((al) => {
              if (Math.round(diffDays) <= 0) {
                al.textContent = "Disable";
                el.textContent = "0 Days";
              } else {
                al.textContent = "Active";
              }
            });
          }
        });

        // status.forEach((el) => {
        //   const utcDateStr = el.getAttribute("data-status");
        //   if (utcDateStr) {
        //     const originalDate = new Date();
        //     const oneMonthLater = new Date(utcDateStr);
        //     oneMonthLater.setMonth(oneMonthLater.getMonth() + 1);

        //     const diffTime = oneMonthLater - originalDate; // difference in milliseconds
        //     const diffDays = diffTime / (1000 * 60 * 60 * 24); // convert to days

        //     el.textContent = `${Math.round(diffDays)} days`;

        //     if (Math.round(diffDays) <= 0) {
        //       el.textContent = "Disable";
        //     } else {
        //       el.textContent = "Active";
        //     }
        //   }
        // });
      }

      // Run after DOM is loaded
      window.addEventListener("DOMContentLoaded", convertDatesToLocal);
    </script> -->
  </body>
</html>
