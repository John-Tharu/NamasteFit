<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Subscribe – NamasteFit</title>
    <link rel="stylesheet" href="/style.css" />
    <script
      src="https://kit.fontawesome.com/bb7a749298.js"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <%- include('partials/nav') %>

    <main class="subscribe-section">
      <h1>Subscribe Now</h1>
      <form class="subscribe-form" action="/subscription" method="POST">
        <div class="form-group">
          <label for="name">Full Name</label>
          <input
            type="text"
            value="<%= name %>"
            id="name"
            name="name"
            readonly
          />
        </div>

        <div class="form-group">
          <label for="email">Email Address</label>
          <input
            type="email"
            value="<%= email %>"
            id="email"
            name="email"
            readonly
          />
        </div>

        <div class="form-group">
          <label for="plan">Select Plan</label>
          <select id="plan" name="plan" required>
            <% if(title){ %>
            <option value="<%= title %>"><%= title %>/month</option>
            <% }else{ %>
            <option value="">-- Choose a Plan --</option>
            <% } %>
            <option value="free">Free – NPR. 0/month</option>
            <option value="basic">Basic – NPR. 499/month</option>
            <option value="standard">Standard – NPR. 999/month</option>
            <option value="premium">Premium – NPR. 9999/month</option>
            <option value="annual">Annual – NPR. 11111/year</option>
          </select>
        </div>

        <button type="submit" class="btn">Proceed to Payment</button>
      </form>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>
